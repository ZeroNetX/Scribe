name: scribe
description: Blog from ZeroNetX Team.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  google_fonts: ^3.0.1
  js: ^0.6.4
  zeronet_ws: ^0.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 2.0.0